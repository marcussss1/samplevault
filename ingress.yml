apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: samplevault-ingress
spec:
  rules:
    - host: samplevault.ru
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: samplevault
                port:
                  number: 8000
